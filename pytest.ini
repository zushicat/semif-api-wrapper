[pytest]
# Integration tests hit the live source engine; run them explicitly with
# `pytest -m integration` (auto-skip when the engine is unreachable).
addopts = -m "not integration"
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    integration: tests that hit the live source engine (deselected by default)
testpaths = tests
